@model IEnumerable<ZonaRTX.Models.ProductosProveedoresModel>

@{
    ViewData["Title"] = "Productos y Proveedores";
}

<h1 class="text-center my-4">@ViewData["Title"]</h1>

<div class="mb-4">
    <a asp-action="Create" class="btn btn-success">
        <i class="bi bi-plus-circle"></i> Crear Nueva Relación
    </a>
</div>

<table class="table table-hover table-striped">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.id_producto)</th>
            <th>@Html.DisplayNameFor(model => model.id_proveedor)</th>
            <th>@Html.DisplayNameFor(model => model.precio_compra)</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.id_producto)</td>
                <td>@Html.DisplayFor(modelItem => item.id_proveedor)</td>
                <td>@Html.DisplayFor(modelItem => item.precio_compra)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.id_producto" class="btn btn-warning btn-sm">
                        <i class="bi bi-pencil-square"></i> Editar
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.id_producto" class="btn btn-danger btn-sm">
                        <i class="bi bi-trash"></i> Eliminar
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
